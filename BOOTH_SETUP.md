# 🪧 ระบบลงทะเบียนผู้ประจำบูธเครือข่าย

ผู้ประจำบูธ (บูธละ 2 ท่าน) **สแกน QR ของบูธตัวเอง → กรอกชื่อ → ลงทะเบียน** เสร็จในไม่กี่วินาที
เจ้าหน้าที่ดูภาพรวมได้ว่าบูธไหนมาครบ/ใครประจำบูธไหน แบบเรียลไทม์

## ไฟล์ในระบบ

| ไฟล์ | ใช้ทำอะไร | ใครเปิด |
|------|-----------|---------|
| `booth-register.html` | หน้าลงทะเบียน (เปิดจากการสแกน QR `?b=<รหัสบูธ>`) | ผู้ประจำบูธ (มือถือ) |
| `booth-admin.html` | แดชบอร์ดภาพรวมทุกบูธ + Export CSV | เจ้าหน้าที่ |
| `booth-qr.html` | สร้าง/พิมพ์ QR ของแต่ละบูธ | เจ้าหน้าที่ (ก่อนงาน) |
| `booth-config.js` | **ตั้งค่าทั้งหมด** — รายชื่อบูธ, จำนวนคน/บูธ, Supabase | ผู้ดูแล |
| `booth-setup.sql` | สร้างตารางในฐานข้อมูล Supabase | ผู้ดูแล (ครั้งเดียว) |

---

## เริ่มใช้งาน 4 ขั้น

### 1) แก้รายชื่อบูธ
เปิด `booth-config.js` แล้วแก้ `BOOTHS` ให้ตรงกับงานจริง (เพิ่ม/ลบได้ตามจำนวนบูธ):
```js
BOOTHS: [
  { id: '1', name: 'ชื่อบูธจริง', org: 'หน่วยงาน' },
  ...
]
```
> `id` ต้องไม่ซ้ำ และ **ห้ามเปลี่ยนหลังพิมพ์ QR แล้ว** (เพราะค่านี้ฝังอยู่ใน QR)
> ปรับจำนวนคนต่อบูธได้ที่ `STAFF_PER_BOOTH` (ค่าเริ่มต้น = 2)

### 2) ตั้งฐานข้อมูล (Supabase)
เปิด **Supabase → SQL Editor → New query** วางทั้งไฟล์ `booth-setup.sql` → **Run**
> ถ้ายังไม่ทำขั้นนี้ ระบบจะใช้ **“โหมดทดลอง”** เก็บข้อมูลในเครื่องไปก่อน (ข้อมูลจะไม่รวมกันข้ามอุปกรณ์)
> จะใช้ Supabase โปรเจกต์อื่น ก็แก้ `SUPABASE_URL` / `SUPABASE_KEY` ใน `booth-config.js`

### 3) นำขึ้นเว็บ แล้วพิมพ์ QR
- อัปไฟล์ทั้งหมดขึ้นโฮสต์ (GitHub Pages / Netlify / โฮสต์เดิม — วางรวมโฟลเดอร์เดียวกัน)
- เปิด `booth-qr.html` → ตรวจ Base URL ให้เป็นลิงก์จริงของ `booth-register.html` → **พิมพ์ทั้งหมด** → ตัดติดหน้าบูธ

### 4) วันงาน
- ผู้ประจำบูธสแกน QR ที่บูธตัวเอง → กรอกชื่อ → ลงทะเบียน
- เจ้าหน้าที่เปิด `booth-admin.html` ดูสด ๆ ว่าบูธไหนมาครบ และกด **CSV** เพื่อดึงรายชื่อ

---

## เกร็ดเล็กน้อย
- หน้า register รองรับเปิดแบบไม่มี `?b=` ด้วย — จะมีหน้าให้ **เลือกบูธเอง** (เผื่อทดสอบ)
- กันชื่อซ้ำในบูธเดียวกัน (เตือนก่อน แต่ยังลงซ้ำได้ถ้าจำเป็น) และจำว่าอุปกรณ์นี้เคยลงบูธไหนแล้ว
- แดชบอร์ดรีเฟรชอัตโนมัติทุก 10 วินาที (เฉพาะตอนต่อฐานข้อมูล Supabase)
- รีเซ็ตข้อมูลก่อนเริ่มงานจริง: `delete from public.booth_staff;` ใน SQL Editor
